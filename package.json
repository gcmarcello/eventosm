{
  "name": "eventosm-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --no-daemon",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "1.10.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.1.3"
}
